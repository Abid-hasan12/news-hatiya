{
  "name": "news-hatiya",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://abid-hasan12.github.io/news-hatiya/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "deploy": "npm run build && git --git-dir=dist/.git --work-tree=dist init && git --git-dir=dist/.git --work-tree=dist branch -m gh-pages && git --git-dir=dist/.git --work-tree=dist add . && git --git-dir=dist/.git --work-tree=dist commit -m \"deploy\" && git --git-dir=dist/.git --work-tree=dist push -f origin gh-pages"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^9.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.16"
  }
}